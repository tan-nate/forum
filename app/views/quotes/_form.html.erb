<%= form_with model: @quote, class: "new_quote" do |f| %>
    <%= f.hidden_field_tag 'user_id', '#{current_user.id}'  %>
    
    <%= f.text_area :text %>
    <%= f.text_field :source_url %>
<% end %>